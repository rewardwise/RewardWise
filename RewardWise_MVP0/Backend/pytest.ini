[pytest]
# app/rag/test_kb.py is a manual smoke script (python -m backend.rag.test_kb),
# not a pytest module. ignore-glob with single * (fnmatch, no recursive glob)
# is location-independent across invocation contexts.
addopts = --ignore-glob=*app/rag/test_kb.py
